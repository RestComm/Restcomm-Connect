'use strict';

var rcMod = angular.module('rcApp');

// Numbers : Incoming : List ---------------------------------------------------

rcMod.controller('MainCtrl', function ($rootScope, $scope, $dialog, $http, $location, SessionService, $log) {
  $log.debug("Entered MainCtrl...");
});
