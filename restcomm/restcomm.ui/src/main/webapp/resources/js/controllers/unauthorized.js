'use strict';

/**
 * Controller definition for the '/unauthorized' view. It covers the case when a user has a valid token
 * but does not have access to this restcomm instance.
 */
angular.module('rcApp').controller('UnauthorizedCtrl', function ($scope) {
});
