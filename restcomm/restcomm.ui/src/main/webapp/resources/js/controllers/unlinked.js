'use strict';

/**
 * Controller definition for the '/unlinked' view. It covers the case when a user is granted
 * access to an instance but is not linked to a Restcomm account.
 */
angular.module('rcApp').controller('UnlinkedCtrl', function ($scope,AuthService) {
});
